# 🌸 Flower Classification using Deep Learning

>A deep learning project that classifies flower images into multiple categories using a Convolutional Neural Network (CNN).
>The model is trained in Python using TensorFlow/Keras, and the full workflow covers dataset preparation, training, evaluation, and predictions.

---

## 🛠️ Features

- Image classification using CNN
- Custom Data Set For Train, validate, and test dataset splits
- GPU supported (if available)
- Metrics visualization
- Prediction on custom images

## How To Run
 
 - Clone The Repo

```bash

# Create Conda environment
conda create -n FlowerClassification python=3.13.5 -y

# Activate environment
conda activate FlowerClassification

# Install dependencies
pip install -r requirements.txt

```